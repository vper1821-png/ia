# Text-to-SQL Agent for WMS

Agente que convierte lenguaje natural a SQL usando phi3:mini y LangChain.

## Despliegue automático

La imagen se construye con GitHub Actions y se publica en ghcr.io.

Ver los archivos en `k8s/` para desplegar en Kubernetes.
